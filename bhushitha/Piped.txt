import java.io.*;
public class Piped
{
    public static void main(String[] args) throws IOException
    {
        PipedInputStream s_input = new PipedInputStream();
        PipedOutputStream s_output = new PipedOutputStream();
        try
        {
           
            s_input.connect(geek_output);
 
            
            s_output.write(65);
            s_output.write(99);
            s_output.write(42);
            s_output.write(55);
            s_output.write(71);
            pipe_output.flush();
            System.out.println("Use of flush() method : ");
 
            int i = 5;
            while(i > 0)
            {
                System.out.print(" " + (char) s_input.read());
                i--;
            }
 
            
            System.out.println("\nClosing the Output stream");
            pipe_output.close();
 
        }
        catch (IOException except)
        {
            except.printStackTrace();
        }
    }